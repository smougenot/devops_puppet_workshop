---

#
#Exemple de fichier de mot de passe prêt à être chiffré via hiera-gpg
#

# Base de données de PE, login l_pe
bdd_pe_password           : 'l_pe_prod'

# Base de données de PS, login l_ps
bdd_ps_password            : 'l_ps_bdd_prod'

# PS, login admin
ps_password                : 'l_ps_prod'

# JMX : mot de passe pour un accès en lecture seule (Read Only)
jmx_ro_pwd                 : 'password'

# JMX : mot de passe pour un accès complet (Read Write)
jmx_rw_pwd                 : 'password'
